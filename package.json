{
  "name": "profit-plus",
  "version": "1.0.0",
  "description": "Profit Plus full-stack application",
  "scripts": {
    "start-backend": "npm --prefix backend run start",
    "start-frontend": "npm --prefix frontend run start",
    "build-backend": "npm --prefix backend install && npm --prefix backend run build",
    "build-frontend": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "concurrently \"npm run start-backend\" \"npm run start-frontend\"",
    "build": "npm install && npm run build-backend && npm run build-frontend"
  },
  "dependencies": {
    "concurrently": "^7.5.0"
  },
  "devDependencies": {},
  "engines": {
    "node": "20.x"
  }
}
